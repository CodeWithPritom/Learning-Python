name = "Pritom is a Good  boy  "

print(name.replace("  ", " "))# replace the double space with a single space  
print(name) #String is immutable so the original string will not change by executing the above line of code. To change the original string we need to assign the new string to the original variable name.