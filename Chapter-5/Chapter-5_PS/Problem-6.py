d = {}

name = input("Enter friends name: ")
lang = input("Enter friends favourite language: ")
d.update({name: lang}) # This line updates the dictionary 'd' by adding a new key-value pair where the key is the friend's name and the value is their favourite language. If the friend's name already exists in the dictionary, it will update the existing entry with the new favourite language.

name = input("Enter friends name: ")
lang = input("Enter friends favourite language: ")
d.update({name: lang})

name = input("Enter friends name: ")
lang = input("Enter friends favourite language: ")
d.update({name: lang})

name = input("Enter friends name: ")
lang = input("Enter friends favourite language: ")
d.update({name: lang})

print(d)